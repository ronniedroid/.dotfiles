#

# Shortcuts

super + alt + p
    pmenu.sh

super + shift + f
    firefox
super + shift + e
    emacs
    
super + alt + m
    MonitorLayout.sh
# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd

super + alt + space
    kbdlayout.sh

super + alt + w
  Wallpaper.sh    
#pulse Audio controls

super + Left
    amixer -c 0 -q set Master 2dB- && pkill -RTMIN+10 dwmblocks
super + Right
    amixer -c 0 -q set Master 2dB+ && pkill -RTMIN+10 dwmblocks



# Sreen brightness controls

super + Up
	light -A 5 && pkill -RTMIN+11 dwmblocks
super + Down
	light -U 5 && pkill -RTMIN+11 dwmblocks

# Media player controls

XF86AudioPlay
	playerctl play
XF86AudioPause
	playerctl pause
XF86AudioNext
	playerctl next
XF86AudioPrev
playerctl previous
