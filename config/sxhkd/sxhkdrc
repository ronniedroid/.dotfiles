#

# Shortcuts

super + alt + p
    pmenu.sh

super + shift + f
    firefox

super + shift + e
    emacs

super + alt + m
    MonitorLayout.sh
# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd

super + alt + space
    kbdlayout.sh && pkill -RTMIN+9 dwmblocks

super + alt + w
  Wall-d
#pulse Audio controls

super + {Left,Right}
    pamixer {-d,-i} 5 && pkill -RTMIN+10 dwmblocks

# Sreen brightness controls

super + {Down,Up}
	light {-U,-A,} 5 && pkill -RTMIN+11 dwmblocks

# Media player controls

XF86AudioPlay
	playerctl play
XF86AudioPause
	playerctl pause
XF86AudioNext
	playerctl next
XF86AudioPrev
playerctl previous
