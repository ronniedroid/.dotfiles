# make sxhkd reload its configuration files:

super + Return
		alacritty

alt + Return
		st

super + Escape
		pkill -USR1 -x sxhkd

# Apps shortcuts

super + shift + f
		firefox

super + d
    dmenu_run -h 24

super + shift + e
    emacs

super + shift + t
    thunderbird

super + shift + w
    VBoxManage startvm "Windows 10"

# Scripts shortcuts

alt + shift + p
    pmenu.sh

alt + shift + m
    MonitorLayout.sh && refstatbar

alt + shift + space
    kbdlayout.sh && refstatbar

alt + shift + w
		Wall-d -f -d $HOME/Pictures/Wallpapers/

alt + shift + b ; {a,b,d,y,r}
    bmanager.sh {-a (xclip -o),-b,-d,-y,-r}

alt + shift + comma
    bitwarden-dmenu --dmenu-args='-i -l 10 -p Select_item' --clear-clipboard 60 --session-timeout 440 --sync-vault-after 3600 --on-error 'xargs notify-send --urgency=low'

#pulse Audio controls

super + {Left,Right}
    pamixer {-d,-i} 5 && refstatbar


# Sreen brightness controls

super + {Down,Up}
		light {-U,-A} 5 && refstatbar

# Media player controls

XF86AudioPlay
		playerctl play
XF86AudioPause
		playerctl pause
XF86AudioNext
		playerctl next
XF86AudioPrev
		playerctl previous
