#!/bin/sh

volstat=$(pamixer --get-volume)

vol=$(echo "$volstat")

if [ "$vol" -gt "70" ]; then
	icon=""
elif [ "$vol" -lt "30" ]; then
	icon=""
else
	icon=""
fi

printf "%s%% \n" "^c#282c34^^b#e5c07b^ $icon ^c#abb2bf^^b#282c34^ $vol"
